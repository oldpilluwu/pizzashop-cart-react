import React from "react";
import ProductsList from "../components/Products";

const Products = () => {
    return (
        <div>
            <ProductsList />
        </div>
    );
};

export default Products;
